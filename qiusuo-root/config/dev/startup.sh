'
What needs to be done
(1)Create qiusuo-network using docker
(2)Call docker-compose to startup all the services. We will pack a startup script to all docker image.
So we could call one root startup script to start all the services.
'